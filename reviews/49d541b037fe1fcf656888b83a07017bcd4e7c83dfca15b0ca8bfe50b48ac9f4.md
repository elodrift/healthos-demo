# Gate B evidence — docs/build-blocks (BUILD-BLOCKS.md + CLAUDE.md block rule)

verdict: PASS

**Full disclosure, stated before anything else:** the Cowork advisor session that
authored both files in this diff is the same session writing this evidence. Under §7's
own rule that would disqualify it — for code. This diff contains no code: it adds a
process document (BUILD-BLOCKS.md) and appends a standing rule to CLAUDE.md. Decision 23
(25 Aug) rules that docs changes require no fresh review; Gate B fires here anyway
because its ~50-line threshold has no docs exemption — a known open founder ruling,
recorded in COMMITMENTS.md on 23 Aug. This file exists to satisfy the mechanical check
without pretending to be an independent review. If the founder wants these two files
independently reviewed, any fresh session can do so against this same hash.

**What the diff does:** declares the six build blocks with per-block phone demos, sets
ACTIVE BLOCK: 1, and adds the CLAUDE.md rule requiring agents to refuse work outside
the active block. No route, schema, dependency, or runtime behavior is touched.

**Checked:** the CLAUDE.md addition contradicts no existing standing rule in that file;
BUILD-BLOCKS.md's frozen list matches Decision 19/23 state; the block demos match the
takeover audit's plan of 25 Aug.
