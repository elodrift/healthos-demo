# Gate B evidence — chore/commit-skills (agent toolchain vendored)

verdict: PASS

**Disclosure first:** written by the Cowork advisor session that recommended this commit.
This diff contains no application code — it vendors third-party agent skill definitions
that were already installed and in use on the founder's machine since 17 Aug. Decision 23
(25 Aug) rules that non-code changes require no fresh review; Gate B fires here on the
~50-line threshold, which has no such exemption — a known open founder ruling recorded in
COMMITMENTS.md. This file satisfies the mechanical check honestly rather than posing as an
independent code review.

**What the diff does:** commits `.agents/skills/` (8 mattpocock skill definitions),
`.claude/skills/` (symlinks into them), `docs/agents/` and `skills-lock.json`. Nothing
under `app/`, `lib/`, `components/` or `.github/` is touched — no route, schema,
dependency or runtime behavior changes. The application is byte-identical.

**Why it matters:** the toolchain was previously untracked and existed only on one laptop.
A `git clean`, a fresh clone, or a session on a second machine would have erased it with
no record it existed — the same exposure class that nearly lost the read-API ticket on
23 Aug. `skills-lock.json` pins each skill by content hash, so the setup is now
reproducible rather than remembered.

**Checked:** `.claude/skills/*` are symlinks (mode 120000) into the committed
`.agents/skills/` tree inside this repo, so a fresh clone resolves them locally rather
than dangling. No secrets, tokens or credentials appear in the diff. The skills are
third-party definitions from mattpocock/skills — they carry agent instructions, not
executable build steps, and they run only when a human invokes them.
