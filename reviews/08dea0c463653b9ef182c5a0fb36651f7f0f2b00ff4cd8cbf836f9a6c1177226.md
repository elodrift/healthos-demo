verdict: PASS

# Review — PR #18, `feat/caption-suite-provenance-walker` at d12b5f9

Fourth and final round. Reviewed at head `d12b5f9af8c29e8312fb3f84adc13dc04851b76e`
(base `main`, 1217-line diff, no sensitive path — line count alone triggers Gate B).
This round: `per100ml` removed from `BASIS_DECLARING_CONTAINERS`, with a tripwire
test pinning that it stays removed.

## Branch integrity — spot-checked, confirmed

`git merge-base --is-ancestor 01a2bc0 d12b5f9` → yes; `git show 01a2bc0` reproduces
exactly the one-line `verify.yml` diff, authored by `elodrift`. History is linear:
`660f080 → 01a2bc0 → ceb741a → d12b5f9`. `671f46f` (the rewritten copy from the
bad-rebase recovery attempt) does exist in the repository — on a stray branch,
`origin/v0/elodrift-1f51217f` — but `git merge-base --is-ancestor 671f46f d12b5f9`
returns false: it is not in this branch's history. `verify.yml` still contains
`npm run test:provenance` in the Offline tests step.

## The tripwire — bites, verified by re-adding the key myself

`{ per100ml: { kcal: 42, carbG: 11 } }` now expects `["per100ml.kcal",
"per100ml.carbG"]`. Re-added `per100ml` to `BASIS_DECLARING_CONTAINERS` directly
(not trusting the commit message) and reran: exactly 1 of 21 cases fails — this
one, and only this one. Reverted; 21/21 clean.

## All four mutations reproduced independently

| mutation | claimed | reproduced |
|---|---|---|
| re-add `per100ml` | 1 fails | 1 fails ✓ |
| empty `BASIS_DECLARING_CONTAINERS` | 4 fail | 4 fail ✓ (both real label fixtures, the nested-object case, the single-array-layer case) |
| revert the array-hop fix (forward `basisDeclared` through arrays unconditionally) | 2 fail | 2 fail ✓ (the double- and triple-array cases) |
| over-correct (reset on every array hop, including the legitimate first one) | 1 fails | 1 fails ✓ (the single-array-layer case — confirms the fix doesn't cry wolf) |

Each mutation applied to the actual file, run, then reverted; final state clean
diff, 21/21.

## The `per100ml` claim, verified independently

`lib/food/barcode.ts:140-143` reads only `energy-kcal_100g`, `proteins_100g`,
`carbohydrates_100g`, `fat_100g` — confirmed by reading the file, never any
`_100ml`-suffixed field. `LabelPer100g`/`BarcodeLookup` carry exactly one field,
always named `per100g`. `LABEL_NO_SERVING` (Sparkling water — a liquid) already
ships under `per100g` and flags clean with `per100ml` gone. The claim holds.

## Attack this round: swept every real `jsonPrivate` call site in the app

With `BASIS_DECLARING_CONTAINERS` down to a single, narrow entry and the array
logic already hardened last round, I looked for a live gap the same way every
real finding in this PR was found — against actual payload shapes, not synthetic
ones. Grepped all four routes calling `jsonPrivate`:

- `app/api/agent/route.ts` — `{ text, source, intent }`, no numeric nutrition fields.
- `app/api/meals/route.ts` — covered in prior rounds, still clean.
- `app/api/barcode/route.ts` — covered, still clean (verified again above).
- `app/api/meal-photo/upload/route.ts` — returns `recognition` (the full
  `RecognitionResult`), which trips the same already-pinned, deliberately
  expected-flagged recognizer gap, reached through a different code path. Not a
  new finding — same root cause, already documented and out of scope for this PR.

Also checked case-sensitivity (`PER100G`, `Per100G` — both still match via
`.toLowerCase()`, no leak) and re-ran both real label fixtures end to end.

No new gap found. This round holds.

## Standards

Typecheck and lint clean (verified in prior rounds; this round's diff is
`naked-numbers.ts` + `scripts/test-provenance.ts` only, both TypeScript already
covered by those checks). No new standards issues.

## CI status — still outstanding, correctly disclosed

`verify.yml` has the required line, but `Offline tests` has not executed once
across any of these four rounds — Gate B fails first and short-circuits the job
every time, including on this exact head. **CI enforcement of `test:provenance`
becomes verifiable only once this PASS lands and Gate B clears on a fresh push —
confirm `Offline tests` actually runs and passes it before merging.** Everything
in this review, and the 21 provenance cases, is locally verified only.

---

**Summary:** four rounds, two real live/latent defects found and fixed (barcode
false positive, `carbG`/`servingG` false negative), one real design escape found
and closed (the array-axis leak), one dead/unverified entry found and removed
with a tripwire against recurrence (`per100ml`). This round's specific asks —
tripwire, mutation count, branch integrity — all reproduced independently and
hold. A full sweep of the app's real `jsonPrivate` surface for a fifth axis found
nothing new. Passing.
